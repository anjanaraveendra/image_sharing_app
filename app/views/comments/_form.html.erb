<%= form_for [commentable, Comment.new] do |f| %>
<%#= raise f.inspect %>
<div class="form-group">
	<%= f.text_area :body, class: "form-control", placeholder: "Add a Comment", id: "comment_box" %>
</div>
<%= f.submit class: "btn btn-primary" %>
<% end %>