<div align="center">

<%= link_to 'New Gallery', new_gallery_path %> | <%= link_to 'New Image', new_gallery_attachment_path %>

<div class="row">
  <div class="spacer"></div>
  <div class="container text-right">
    <% if user_signed_in? %>
        <%= link_to "New Conversation", new_conversation_path, class: "btn btn-info btn-lg" %><br/><br/>
        <%= link_to "Go To Inbox", mailbox_inbox_path, class: "btn btn-primary btn-lg" %>
    <% else %>
        <p>You can log in as default user in one window with email: <strong>default-user@email.com</strong>
        and password: <strong>12345678</strong> then create an account on a separate window and emulate a conversation
        between yourself and the default user.</p>
        <%= link_to "Login", new_user_session_path, class: "btn btn-primary btn-lg" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-warning btn-lg" %>
    <% end %>
  </div>
</div>

<% @galleries.each do |gallery| %>
<div id="dimensions">
<%= link_to image_tag(gallery.gallery_attachments.first.avatar_url), gallery %>
</div>
<%= gallery.title %> | <%= username(gallery.try(:user_id)) %><br/><br/>
<% end %>
</div>
